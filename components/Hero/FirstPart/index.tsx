import TextBlock from "@/ui/textBlock";

const FirstPart = () => {
  return (
    <TextBlock
      title="Unleashing the World of 3D on the Web"
      description="U can try it →"
    />
  );
};

export default FirstPart;
